{
  "id": "longest-common-prefix",
  "title": "Find Longest Common Prefix",
  "difficulty": "Easy",
  "tags": [
    "string"
  ],
  "description": "Find the longest common prefix string amongst an array of strings. Return empty string if no common prefix.",
  "examples": [
    {
      "input": "strs = [\"flower\",\"flow\",\"flight\"]",
      "output": "\"fl\""
    }
  ],
  "starterCode": "function longestCommonPrefix(strs) {\n  // your code here\n}",
  "solution": "function longestCommonPrefix(strs) {\n  if (!strs.length) return '';\n  \n  let prefix = strs[0];\n  \n  for (let i = 1; i < strs.length; i++) {\n    while (strs[i].indexOf(prefix) !== 0) {\n      prefix = prefix.substring(0, prefix.length - 1);\n      if (!prefix) return '';\n    }\n  }\n  \n  return prefix;\n}",
  "hints": [
    "Start with first string as prefix",
    "For each string, trim prefix until it matches",
    "Return empty if no common prefix"
  ],
  "testCases": [
    {
      "input": [
        [
          "flower",
          "flow",
          "flight"
        ]
      ],
      "expected": "fl"
    },
    {
      "input": [
        [
          "dog",
          "racecar",
          "car"
        ]
      ],
      "expected": ""
    }
  ],
  "functionName": "longestCommonPrefix",
  "xpReward": 70
}